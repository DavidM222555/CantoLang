
func addNums(arg1: int, arg2: int, arg3: int) -> int {
    func multBy2(arg4: int) -> int {
        return arg4*2;
    }

    // Nested function use
    print(multBy2(arg1) + multBy2(arg2) + multBy2(arg3));
}

addNums(10, 10, 10);